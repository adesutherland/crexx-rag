# Tests 1–5 repeat — committed baseline 696858c

The user requested a baseline commit followed by Tests 1–5. Commit
`696858c0a327d10594ecb6fcc5a88d5202ffb7be` contains the completed replay repair
and Test 5 follow-ups. Its unchanged native artifact already passes 71/71 local
tests. This run exercises the live smoke workflows on a new disposable restore
of the retained pre-Test-1 backup. The master corpus, permanent ScottishHistory
copy and normal installation remain outside this run.

**Complete: all five smoke workflows ran.** Operational recovery and MCP checks
pass. Test 3 retains one rejected content decision and two unresolved decisions;
Test 4 exposes a separate figurative-place classification issue. These are not
an all-content-correct result. The follow-ups below remain explicit.

## Checklist and intended behavior

- [x] **B0 — Commit the baseline.** Record the clean commit and freeze the tested
  executable; native SHA-256 is
  `88b2fe4a045c42f6a514323bd93804761256b77a15928876e95b5557154bf9c7`.
- [x] **B1 — Restore the starting data.** Public restore/migration; confirm the
  five missing embeddings and absent Bannockburn source. Record original source,
  attempt and provider state for independent final comparison.
- [x] **Test 1 — Missing embeddings.** Repair exactly the five original public
  history occurrences using Gemini `gemini-embedding-2`: at most five calls,
  $0.005 configured combined cap and a fresh five-minute window; no reasoning.
- [x] **Test 2 — New document.** Import the same 952-byte public Bannockburn
  excerpt. Five minutes, eight items/calls, at most four managed Codex
  `gpt-5.6-luna` turns, $0.005 API cap, two configured workers. Check independent
  vector activation, retrieval/citation, completed-job retry and repeat-import
  no-op. Failed work affects its item; stored embeddings remain searchable,
  existing coverage stays usable, and restart/retry finishes outstanding work
  and index activation.
- [x] **Test 3 — Maintenance.** Fresh bounded automatic window: five minutes,
  eight items/calls, at most four managed Codex turns, $0.005 API cap, two workers.
  Inspect actual selected public-source tasks, accepted/refused decisions,
  preserved uncertainty, receipts and stopped workers. Budget closure does not
  turn unresolved content into success.
- [x] **Test 4 — Selected extraction holds.** Replay only the original Fiers and
  Nairn holds under the normal configuration including Bannockburn. At most four
  managed Codex turns combined (initial plus at most one correction each), zero
  Gemini calls and zero monetary API cost, within a fresh five-minute stage.
  Validate/review the resulting content, preserve original histories and test
  zero-call completed-job retry. Reject unsupported claims; do not weaken
  grounding or remove unrelated sources to make replay work.
- [x] **Test 5 — MCP Q&A.** Repeat the shared QA workflow: status/source scope,
  lexical search, directional paths, full citations, assistant-composed answer,
  and default/13/200/201 passage checks. No product answerer or provider calls.
  Retain CPU/work/payload observations; the shared computer precludes stopwatch
  acceptance gates or a causal speedup claim.
- [x] **B2 — Close and report.** Verify final source/history preservation, vector
  coverage, provider accounting and terminal jobs/workers. Record each result
  honestly, including content holds or any retry needed. Update the handoff.

The fresh windows above repeat the user-authorized test scopes. Saved historical
plans are evidence only; each new job uses a fresh public plan or selected replay.
No broader unfinished ingestion or historical maintenance window is restarted.

Evidence: [run identity](qa/smoke-tests-1-5-20260914/identity.json).

## Results

Test 1 passed: five processed items, five successful Gemini calls, $0.000208
recorded cost, no reasoning calls, two clean workers and automatic vector
publication. Observed execution was 38.20 seconds. No manual recovery was needed.

Test 2 passed: all four items processed with two Gemini calls ($0.000049) and
two successful Codex turns; no correction, worker failure or manual recovery.
Automatic vector activation succeeded. Existing-source search worked during
processing. The new battle passage remains fourth in the twelve-passage result;
its citation exactly matches bytes 502–951. Completed-job retry succeeded in
4.71 observed seconds, and reimport returned `identical-no-op`. Main execution
was 84.13 observed seconds; provider receipts account for 41.397 seconds.

Test 3 completed operationally with a content hold: four Codex turns, zero
Gemini/API cost, one resolved task, two explicitly unresolved decisions and one
rejected identity decision. Two stale items were skipped; two unstarted items
were cancelled at window closure and remain pending tasks. The job truthfully
reports `completed_with_errors`. Both workers stopped cleanly; vector activation
was `identical-no-op`. No allowance renewal or forced content acceptance was
used. Observed execution was 28.10 seconds; provider work totalled 42.973 seconds
across the parallel calls.

Test 4 passed its selected operational recovery and grounding checks. Fiers
completed in one Codex turn; Nairn completed after its ordinary single correction
omitted an unsupported cavalry membership. The supported advanced-guard
membership of the Argyleshire men was retained. Both roots resolved under the
normal configuration including Bannockburn; no configuration workaround, new
review decision or manual claim publication was needed. The combined live stage
was 11:42:31–11:44:40 UTC, three Codex turns, zero Gemini calls/API cost. All 122
original grounding outcomes and 45 draft outcomes reproduce unchanged.

Content quality still needs attention: the Fiers response classifies the
figurative phrase “Siberian solitude” as a place. Mechanical quotation/label
validation does not establish a literal geographic entity. This is recorded
as a content-quality follow-up, not hidden by the successful replay result.

Both completed Test 4 jobs also pass ordinary retry with zero new attempts or
provider runs (4.54 and 0.70 observed seconds). Original selected item records
are identical to the pre-Test-1 backup. The bounded live window is closed.

Test 5 passed **59 actual MCP requests**: 53 in the ordinary QA/path/citation
session and six boundary/breadth checks. There were zero RAG provider calls,
zero ordinary QA writes and no corpus/generation changes. Explicit paths added
one normal ambiguity gap. Both servers exited cleanly. The assistant composed
the [retained answer](qa/smoke-tests-1-5-20260914/assistant-answer.md) from resolved
source spans, without invoking the product answerer or full overview.

The default returns 12 passages, explicit 13 returns 13, and explicit 200 returns
48 under the unchanged 48-candidate policy. The broad Scotland evidence packet
is 224,580 bytes. Limit 201 is rejected. The direct Bannockburn passage remains
fourth and is retained by the ordinary default; this is not a ranking defect.

Server CPU samples: 18 successful inspections have a median of approximately
0.085 seconds; six path calls, 0.06 seconds; 19 citation reads, 0.01 seconds.
These are CPU observations, not user-visible answer latency or a speed guarantee.
No stopwatch threshold was used on the shared computer.

## Final state and accounting

Public verification passes at schema 19, generation 24,928, with an aligned
manifest and zero repository issues. The nine sources and 34,907 chunks have
complete published vector coverage. All original source/revision/chunk rows,
all original attempts/provider receipts and the two original replay items are
unchanged. Every new run is accounted for by the five smoke jobs.

The ledger increased **82,544 → 82,560**: seven Gemini embedding calls and nine
managed Codex turns. Total monetary API cost is **$0.000257**, plus subscription
usage. All reservations are clear; all 15 recorded test runtime instances are
stopped and their PIDs are absent. No manual index repair or configuration
workaround was needed. Source code and the frozen native artifact are unchanged
from the committed baseline; these new results are follow-up documentation.

## Content follow-ups

- [ ] **C1 — Fiers figurative classification.** Review the newly created
  `Siberian solitude` place concept against the actual comparison in the source,
  and correct its classification through the ordinary curation workflow. Review
  extraction handling of figurative comparisons before claiming that every
  mechanically grounded mention is a literal entity. Evidence is the retained
  Fiers input and response; the concept did not exist in the starting backup.
- [ ] **C2 — Argyll identity selection.** Review the rejected target selection
  for task `task:902129caf64ac0c5426cd694dc905ee950b4a5570b4d06776aa1bcec2b98b87b`
  before an ordinary retry. The response chose a target outside the three
  supplied identity candidates; it was not applied. That target does appear in
  the wider supplied catalogue, so inspect prompt/selection-rule agreement
  before attributing the failure solely to the model. Preserve its receipt and
  the two explicit unresolved decisions rather than treating this bounded
  window as complete content resolution.
