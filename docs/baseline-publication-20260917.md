# Maintenance baseline publication — 17 September 2026

Adrian authorised committing/publishing the qualified baseline, installing it in
ScottishHistory and running one fresh fifteen-minute maintenance soak. Publish
main; no tag or release is requested. Existing runs remain closed.

## Scope and qualification

This baseline includes the approved observability, prompt/selected-occurrence
repairs, checkpoint/restart follow-up, short resolution references and quotation
fallback, and ESC-OPS-02 advanced-call accounting. The dated delivery records
and sole master ROADMAP retain their individual acceptance boundaries.

All 126 required local cases pass in the fresh exact-input audit. The last full
selection took 554.61 seconds: 121 executions and five retained passing receipts.
No unchanged functional test is repeated for publication. The documentation-contract
selection retained its exact-input pass (0.06s); this new dated narrative does
not change its declared inputs. The final audit confirms 126 passed. The explicit
scale lane and hosted/platform/endurance gates are separate.

Qualified native SHA-256:
`b2005232c334127351318e115fd6790eed5bd05b922d162526bd94dcb5fdd4a7`.
Qualified linked SHA-256:
`ab3cac9c454496cd75f0fb652f9c344f96b3955536e4e39a10e0dd3a18e7bbaf`.

## Publication and installation record

The commit containing this record is the source baseline. Record its exact SHA,
remote verification and installed artifact hashes in the Scottish run directory
`/Users/adrian/Documents/ScottishHistory/reports/maintenance-budget-soak-20260917/`.
Install the already-qualified artifacts without rebuilding into the normal user
prefix and ScottishHistory/tools, preserving previous RAG packages and taking a
public corpus backup before processing. Fresh installed CLI/MCP checks precede
any calls. Configuration, models and schema remain unchanged by installation.

## Bounded live acceptance

The run record owns one fifteen-minute window, eight workers, Luna Low normal /
Sol Medium advanced, 24 replacements per hour, zero paid API spend, existing
call/token limits and a one-percent allowance floor. Reuse the sole five-minute
monitor. Restore the temporary spend setting, verify once after drainage and
inspect original receipts. Let normal selection reconsider the previously
premature failed task; do not reset or force it. All advanced calls must count,
including evidence search/read and correction; total job usage retains ordinary
calls too. A short successful run cannot establish full historical accuracy,
long endurance, hosted Gemini or cross-platform qualification.

## Completed publication, installation and live repeat

Source/runtime commit `4a9a8c318196fc7c8fb4ac597d49f565fc3633bb` was pushed to
origin/main, independently checked and installed without rebuilding in ~/.local
and ScottishHistory/tools. Both artifact hashes above match at both prefixes.
Previous RAG packages and a public generation-pinned corpus backup are retained.
Four installed CLI checks and four fresh MCP calls passed. Schema19/generation 29317
were unchanged by installation. No tag/release created.

The [authorised fifteen-minute repeat](maintenance-budget-soak-20260917.md) is
closed: original policy restored, workers/reservations drained, verification
passed at generation 29385. ESC-OPS-02 live acceptance passed. ESC-OPS-03 lifecycle
recording remains open, as do the specified grounding/prompt and wider acceptance
boundaries. Final documentation-only qualification and the resulting closeout
commit are recorded beside candidate.json in the Scottish run directory; the
runtime source identity above remains unchanged.
