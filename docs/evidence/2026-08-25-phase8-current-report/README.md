# Phase 8 current-report reconciliation

Date: 2026-08-25

Status: maintained Phase-8 report complete; Gate 8 programme closeout remains
not satisfied. Phase 8 remains intentionally not a tutorial.

## Reconciliation

The 2026-08-24 report correctly withheld donation, cutover, release and
retirement authority, but two implementation descriptions became stale after
the Phase-3 through Phase-7 application extensions. The maintained report now
records that, for the accepted macOS scope:

- cREXX hosted response completion is repaired;
- configured extraction and embedding work is publicly dispatched and drained;
- native human, JSON and MCP provider smoke tests exact output contracts; and
- real Gemini plus contained Codex/local application routes pass their bounded
  qualification.

Those results improve the `rxllm` candidate evidence and the replacement
surface. They do not supply provider-lifetime/stream/cancel ownership, a second
consumer, an installed generic package, exact Linux, production-shaped
same-session comparison, upstream acceptance or cutover approval.

## Clean-surface decision

- runtime command: not added;
- Phase-8 tutorial: not added;
- library or provider mutation: not performed; and
- report navigation: maintained through the documentation map and status
  pages.

Donation/adoption/compatibility/retirement state belongs to repository
evidence, candidate metadata, upstream ownership and explicit approvals. A
runtime `crexxrag report` verb would duplicate that state and risk presenting a
stale recommendation as authority. The enduring Phase-8 human surface is the
maintained report plus its automated audit.

## Authority audit

- donation submission: not performed and not authorized;
- upstream issue, branch, pull request or coordination write: not performed;
- CREXX sibling edit: not performed;
- cREXX-default cutover: not performed;
- compatibility release/window: not started;
- native deletion: not performed and not authorized;
- release/tag/push: not performed; and
- exact downstream Linux: open.

## QA

The strengthened `phase8_opportunity_report` audit checks the current report,
this reconciliation, the historical evidence, every candidate's denial
metadata, the no-tutorial rule, the absence of a `report` operation from the
closed application vocabulary, and the absence of superseded blocker text.

The focused, proportionate documentation/candidate wall passed:

```text
donation_docs_audit          passed
p2_09_donation_bundles      passed
phase7_qualification        passed
phase8_opportunity_report   passed
100% tests passed, 0 tests failed out of 4
Total Test time (real) = 7.41 sec
```

Immediately before this report-only phase, the final executable regression
wall passed 87/87 in 963.41 seconds. Phase 8 changes no cREXX/C/C++ executable
source, command vocabulary, provider path, schema or build topology; it updates
the maintained report and strengthens its audit. Repeating the full performance
and end-to-end wall would add no executable qualification after that unchanged
baseline. `git diff --check` and the clean local commit boundary are recorded
when this update is sealed.
