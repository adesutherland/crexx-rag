# P3R-08 — Gate 3R Closeout

Status: accepted by Adrian for the recorded macOS/Google Gemini scope on
2026-08-25. The tested implementation is local and uncommitted on top of
`4fd0d9fad1315e12b93728384abe95c7064e7295`; it is not pushed or released.

The enduring native package now performs the human Phase 3 flow itself:
configuration discovery, library initialization, reviewed ingestion, durable
two-process work, configured Gemini extraction and embedding, deterministic
claim validation, vector-generation publication, job reconciliation, evidence
query, unchanged replay and integrity verification.

The final defects were closed as follows:

- unchanged apply returns `identical-no-op`, a null/omitted human job id and
  zero workers/provider calls;
- controller failure preserves a bounded worker runtime detail instead of a
  blank `ERROR:`;
- a completed filtered ingestion job automatically publishes the exact
  immutable `.rxvec` generation and reports its state; and
- the known upstream CREXX `LINEIN()` double-Enter limitation is accepted for
  now and is not duplicated as a new integration issue.

## Evidence

- native linked application and package build passed library initialization and
  two-worker supervision;
- `ctest -L phase3`: 4/4 passed in 51.29 seconds, covering P3R config, process,
  native Gemini product ingestion and the retained dual-VM/mode reconciler;
- `crexx_rag_application_smoke`, `p1_llm_01` through `p1_llm_05`, and
  `p2_07_command_contract`: 7/7 passed in 77.40 seconds, covering the installed
  image, reusable provider contracts/protocols/privacy/retry/secrets, and stable
  human/JSON/NDJSON rendering;
- the fresh native tutorial made exactly two successful first-attempt hosted
  calls: `gemini-3.5-flash-lite` generation and `gemini-embedding-2` embedding;
- the live job completed 2/2 with zero failures/dead letters and balanced
  reservations, accepted one cited directional claim, stored one 768-dimensional
  embedding, published one vector generation and verified with zero issues;
- unchanged live replay queued zero items, returned no job id, launched no
  workers and made no additional provider call; and
- the fresh workspace credential-value scan found zero matches.

The human query remains lexical/typed-graph unless a query vector is explicitly
provided, so `vector state: disabled` in that command does not negate the
published ingestion sidecar. Provider-generated query vectors belong to the
retrieval phase.

This closeout deliberately did not rerun performance, soak, Phase 4–8, full
Release/sanitizer or exact Linux qualification. Those are not Phase 3 defect
tests. Native-v1 remains the default oracle until a separate cutover decision;
no push, release, publication, donation or native removal is authorized.
