# P3R-07 — Real Gemini Vertical Slice

Status: accepted pristine bounded live Gate-3R replay on macOS, 2026-08-25.
The recovered 2026-08-24 run remains below as defect history, not gate evidence.

## Pristine Gate-3R replay — 2026-08-25

The native package of the enduring Level-G application ran from a fresh
tutorial workspace through `init`, guided `ingest --yes`, evidence query,
unchanged ingest and `library verify`. Exactly two hosted calls succeeded on
their first attempts:

| Route | Model | Outcome | Reported input | Reported output | Duration |
| --- | --- | --- | ---: | ---: | ---: |
| `gemini-generate` | `gemini-3.5-flash-lite` | succeeded | 916 | 242 | 1,222 ms |
| `gemini-embed` | `gemini-embedding-2` | succeeded | 0 | 0 | 494 ms |

The job completed with two processed items, zero failed attempts, zero dead
letters and balanced two-open/two-close reservations. cREXX accepted one
directional `billingservice --depends-on--> customerdatabase` claim with the
exact `utf8-0-87` citation, stored one 768-dimensional embedding, automatically
published one aligned `.rxvec` generation, and verified the library with zero
issues. The unchanged replay returned `identical-no-op`, queued zero items,
omitted a job id, started no workers and made no provider call. A scan of the
fresh tutorial workspace found zero credential-value matches. Actual recorded
cost was 879 USD microunits, below the approved $0.05 ceiling.

This clean replay satisfies P3R-07 for the recorded provider/platform scope.
It does not qualify Linux, release, cutover, or provider-generated query-vector
retrieval.

## Pre-call authority and bounds

The user approved Google Gemini as the preferred provider and approved the
Phase-3 implementation sequence, including a real LLM validation. This live
slice is limited to:

- one redistributable public synthetic IT-architecture source;
- exactly two hosted calls: one `gemini-embedding-2` embedding and one
  `gemini-3.5-flash-lite` structured extraction;
- one attempt per call and one in-flight call;
- at most 8,192 input tokens and 1,024 output tokens;
- at most 50,000 USD microunits ($0.05);
- a two-minute job budget and 60-second per-call timeout; and
- only the symbolic credential reference `env:GEMINI_API_KEY`.

The run uses the native package of the enduring Level-G `crexx-rag`
application. It may retain normalized public command results, hashes,
provider/model identity, usage, cost, latency, reconciliation, and stable
citation. It must not retain the credential, authorization headers, an
environment dump, or unrestricted provider response bodies.

## Earlier recovered result — 2026-08-24

The native package of the enduring Level-G application completed the public
sequence:

```text
library.init: schema_version=4
ingest.apply: generation=2 sources_observed=1 items_queued=2 provider_calls=0
worker.run: claim extraction processed
job.retry: one exact embedding dead-letter requeued after the reservation fix
worker.run: embedding processed
job.status: state=completed processed=2 queued=0 running=0 dead_letter=0
query.evidence: candidate_count=1 claims=1 stable UTF-8 citation=0..87
```

Exactly two hosted requests were admitted:

| Route | Model | Outcome | Reported input | Reported output | Duration |
| --- | --- | --- | ---: | ---: | ---: |
| `gemini-generate` | `gemini-3.5-flash-lite` | succeeded | 868 | 231 | 1,200 ms |
| `gemini-embed` | `gemini-embedding-2` | succeeded | 0 | 0 | 360 ms |

The Gemini embedding response did not report token usage, so the normalized
adapter retains zero rather than inventing a count. The generation settlement
recorded 837 USD microunits; the embedding settlement recorded zero because no
usage-based cost could be derived. Both stayed below the admitted $0.05 bound.

The model proposal became one accepted directional claim:

```text
billingservice depends-on customerdatabase
claim-sha256:3a651eac5dd39354a95c72b06d6b9c60f8b0de969b44708d382ae811690c3b44
crexx-rag:library-8c205943cd72391449547db9:source-sha256:9b13fab891db80f270fe821fe3b8224ead0f65768c7019b098856911589a94af:revision-sha256:c03054e9ad8e5be55f39ab2800e7e3332e98db41ddd1f4c3e54998d650e84855:utf8-0-87
```

The final store has one active claim, one embedding, one active embedding
link, two processed items, two balanced reservation open/close events, and no
dead letter. A credential-value scan of the scratch library, declarative
configuration, and this evidence found zero retained matches.

## Recovered admission defect

The first worker invocation processed extraction, then denied embedding before
opening a socket because every item reserved the job-wide 8,192-token and
50,000-microunit ceilings. That left one dead-letter attempt but made no third
party request. The implementation now persists item-specific reservation
ceilings; embedding uses a source-byte-derived input bound, zero output, and a
small cost bound. A new fenced `job retry --item` command requeued only the
affected item after proving all reservations were closed. The failed attempt
remains in the audit trail; it was not deleted or rewritten.

This is valid evidence that the application/provider/claim/embedding/query
path works with exactly two real Gemini calls. A separately approved fresh
two-call run is still required before claiming a clean first-attempt P3R-07
gate result.

Post-run deterministic integrity qualification also found that the live-build
embedding completion used `processed` as its attempt outcome, while the
repository contract requires `succeeded`. The semantic embedding, link, and
provider settlement were present, but `library verify` would report that one
attempt-state issue. The implementation and permanent P3R-02 test now require
the canonical outcome and zero verification issues. The retained live library
is not rewritten; this is another reason the two-call result is vertical-path
evidence rather than pristine gate evidence.
