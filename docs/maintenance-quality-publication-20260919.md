# Maintenance repair publication and quality run — 19 September 2026

Adrian authorised publication and continuation of the maintenance/quality plan.
The product repair is published to `origin/main` as
`9bd5963841b4e88b990c6f61823a6686a25f68bb`; the remote SHA was checked after push.
This is development-branch publication, with no new release tag or CREXX change.

## Published and installed acceptance

ESC-OPS-03 rejects unchanged lifecycle type corrections before recording and
retains concrete SQL diagnostics. ESC-OPS-04 refreshes correction-call allowance
without changing frozen evidence, references or accounting. RAG-QA-05 retains
cleanup failures and handles verified zombie-only process groups.

The final current-input audit accounts for **132/132 passing required cases**.
The exact qualified executable was installed without rebuilding in `~/.local`
and `ScottishHistory/tools`. Both installed SHA-256 values are
`91ad7e7a23ef9bc80acba01b429a0d3a22facdea55233279be6f51372d0feddb`.
Previous packages are retained. The installed CLI opens the Scottish master at
schema 19, generation 29748, with zero status issues and no live workers.
RAG has no configured hosted workflow; wider platform/endurance qualification
remains separate from the complete local gate.

Detailed evidence and the new run record are under
`/Users/adrian/Documents/ScottishHistory/reports/maintenance-quality-20260919/`.
The master corpus and its selected policy remain unchanged. A public backup
and restore prepare the isolated test library, followed by public verification.

## Bounded continuation

Run one fresh 60-minute automatic maintenance window on that copy, with four
workers, local BGE embeddings, routine Luna Low and advanced Sol Medium. Keep
prompts fixed; zero paid API spend, the existing call/token/turn ceilings,
24 replacements/hour and the one-percent allowance floor apply. Do not ingest,
reset tasks, waive failures or automatically copy the result into the master.

One five-minute monitor follows public job/worker status and significant failures.
The activated deadline is fixed; no renewal or second window. An exited
controller may use ordinary same-job recovery within the original authority.
The historic T7-10 Desktop-resume mechanism remains unproved; current required
controller loss, signal, pipe and restart regressions all pass. This run observes
the supported recovery path without claiming that the host investigation is closed.

After drainage, verify once, report actual ordinary/advanced calls, correction
outcomes, remaining tasks and held uncertainty separately. Review a bounded
sample of 30 original decisions, stratified by model and outcome where present,
with exact source support. Retain a small frozen set for subsequent repeatability
comparison; do not turn accepted output or task counts into proof of accuracy.
Internal defects and prompt/model tuning remain separate worklists in the
master register. The health/social-care corpus remains outside this run.

The live run's `RUN.md` and `run-state.json` own the exact job, deadline, limits,
recovery instructions and closeout progress. Publication is complete; live
quality acceptance remains pending until that record closes.
