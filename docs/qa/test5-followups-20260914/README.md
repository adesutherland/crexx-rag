# Test 5 follow-up evidence — 14 September 2026

See [the workplan](../../test5-mcp-qa.md#follow-up-implementation-checklist--approved-14-september).

`baseline-limits.log`, `baseline-query.log` and `baseline-surfaces.log` reproduce
the old passage ceiling and lexical dependence on an invalid vector profile,
with existing positive controls. The first fixed panel and fixture diagnostics
retain the separate candidate-allocation and SQLite physical-file issues in the
new tests. `focused-final.log` closes those two acceptance tests and the reviewed
MCP metadata test; the other five focused tests passed in `focused.log`.

`metadata-reviewed.json` identifies the complete public metadata change: the
maximum of each of seven query tools changes from 12 to 200 in both access lanes.
No other schema, description or access annotation differs.

`identity.json` names the disposable copy and final native build. `mcp/` is the
ordinary provider-free Test 5 repeat, using status and inventory without a full
overview. `wide-mcp/` adds default/13/200/201 and Unicode breadth checks. Both
capture clients are external diagnostic MCP clients, not product workflows.
SQL reads only audit state; actual retrieval and citations use the MCP surface.
The dedicated lexical path tests may record their normal gap observations in
this copy. The separate 200-passage synthetic fixture is retained as an ordinary
both-VM acceptance in `evidence_methodology_scenario.crexx`.

The full suite passes **71/71 in 647.07 seconds**, reusing the previous temporary
two-slot fixture locks. Its generated CTest file was restored byte-for-byte;
`full-suite-result.json` records that and the unchanged artifact hash.
All 59 MCP requests pass, with zero provider runs/attempts and unchanged corpus
hashes/generation. The ordinary Q&A phase is read-only; explicit paths add one
normal ambiguity gap. Timings are observations
on a shared computer, not gates or a causal speedup measurement. No hosted call,
normal installation, authoritative-library change or commit is part of this run.
