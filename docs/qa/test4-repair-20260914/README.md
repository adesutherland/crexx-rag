# Test 4 repair evidence

See [the completed checklist](../../test4-repair-delivery-20260914.md).

- `baseline-replay.log` records the four actual false compatibility rejections;
  `baseline.log` also retains the first fixture setup error. The qualified source
  spelling is covered by `baseline-qualified-scope.log`.
- `semantic-baseline.txt` retains the old semantic identity bytes. Focused and
  full-suite logs cover both VMs, configuration compatibility, lifecycle, prompt
  contracts and existing provider/grounding controls.
- `build-final.log`, `full-suite-result.json` identify the final native artifact
  and 71/71 qualification. The generated CTest file was temporarily given the
  retained fixture locks and restored byte-for-byte; its raw copy is evidence.
- `captured-*` and `nairn-rejected.json` / `nairn-corrected.json` isolate the
  original grounding failure and its minimal mechanical correction through the
  real application validator with a local captured App Server response.
- `nairn-reviewed.json` and `reviewed-*` are the conservative four-relationship
  correction and its final-artifact validation on a separate fixture copy.
- `live-scope.json`, `live-replay.json`, `live-run.json`, `live-status.json`,
  `live-provider-runs.json` and `live-end.txt` retain the separate real one-turn
  run. `live-response-1.json` is the actual response, not the manual draft.
- `live-semantic-review.json`, `live-reviews-before.json` and `live-reject-review-*`
  show that the two incorrect membership proposals were held, then rejected
  through public review. Only the supported Argyleshire membership was published.
- `live-final-accounting.json` proves source/item/attempt preservation and final
  accounting. `live-followup-checks.json`, query/citation results and
  `live-verify.json` cover normal zero-call retry, source access and integrity.

The broad initial query did not surface the Nairn passage in its first twelve
results. The focused source phrase did; both responses are retained. This does
not claim to repair the separately recorded general ranking work.

Captured fixture and real provider ledgers are separate. All corpus writes used
public migration, replay, worker and review commands on disposable copies.
Read-only SQLite checks independently audit preservation and accepted content.
No authoritative library, normal installation or provider credential changed.
Raw captured files retain their original whitespace; `SHA256SUMS` covers them.
