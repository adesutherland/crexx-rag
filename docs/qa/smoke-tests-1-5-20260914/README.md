# Tests 1–5 repeat evidence

See [the action checklist](../../smoke-tests-1-5-20260914.md).
`identity.json` records the committed baseline, unchanged frozen native artifact,
new disposable library and original backup. `before.json` records source and
history fingerprints. Public commands own restore, migration, plans, apply,
execution, retry, review and publication; SQL is read-only audit evidence.

Tests 1–4 reuse their existing bounded public-history scopes with fresh plans
and windows. The Test 5 clients are retained diagnostic MCP clients; they remove
provider credentials and disable the separate Codex provider route. Timings are
observations on the shared computer, not acceptance thresholds.

Raw source spans, protocol output and logs retain their exact bytes, including
source whitespace. Source/documentation whitespace checks exclude this capture
directory. No normal installation, master corpus or permanent query copy changes.

All five smoke workflows completed. `final-accounting.json`, `runtime-final.json`
and `final-verify.json` establish preserved source/history rows, complete vector
coverage, 16 new provider runs, $0.000257 API cost, clear reservations and stopped
processes. `mcp/` and `wide-mcp/` retain 59 successful requests and the unchanged
ordinary QA database. `assistant-answer.md` contains assistant composition and
complete resolved source spans.

The original and draft grounding diagnostics reuse the retained native helper;
its shared `raggrounding` owner is unchanged since that build. All 122 original
and 45 draft outcomes match the prior capture. A wrong diagnostic input path and
an invalid `ingest --scope` invocation are retained as operator-command mistakes;
the corrected commands passed. Neither error executed a provider call.

Content follow-ups remain in the report: the newly created figurative place and
the rejected identity choice whose target is present in the wider catalogue but
not in the three supplied identity candidates. The latter was not applied.
