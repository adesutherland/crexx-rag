# Test 4 — replay compatibility and Nairn grounding

Authority: after completing Test 2, the user requested its local commit and a
move to Test 4. Work in `/Users/adrian/CLionProjects/crexx-rag` on `main`.
The [Test 2 checklist](test2-recovery-delivery-20260914.md) is retired as complete.
This checklist tracks only the remaining selected Test 4 work, not a replay of
all 544 historical extraction roots.

**Complete, 14 September 2026.** T0–T4 are verified. Retain this checklist as
the delivery record. RAG-SMK-010 is repaired; the selected Nairn hold is resolved
through ordinary replay and review. Other historical holds are outside this
bounded test. Changes are uncommitted after baseline `300c2ac`.

## Acceptance checklist

- [x] **T0 — Carry forward the intended workflow.** Preserve completed data,
  immutable attempts and receipts. Retry only unfinished work through ordinary
  commands. A failed extraction must not block available vectors or unrelated
  jobs. Unrelated configuration additions must not prevent a compatible item
  from being retried. Accepted relationships need a continuous source quotation
  supporting their direction and both literal endpoints; correct or omit an
  unsupported relationship rather than weaken grounding.
- [x] **T1 — Establish the latest evidence.** In the 14 September repeat Fiers
  resolved, while Nairn's initial and correction responses both cited cavalry
  for a relationship whose endpoint was the advanced guard. RAG-SMK-010 again
  rejected the unchanged Fiers item after adding the unrelated Bannockburn
  source set. The 13 September Nairn success is a separate historical run.
- [x] **T2 — Reproduce and repair replay compatibility.** Confirm the owning
  configuration projection and callers. Add the missing additive-source
  acceptance before implementation, retaining meaningful source, privacy,
  provider/profile and interpretation-change controls. Replace the whole-job
  veto with shared compatibility for the selected work. Preserve current-target,
  completed-work, immutable-lineage and uncertain-outcome behavior. No temporary
  removal of unrelated sources should be required.
- [x] **T3 — Correct the selected Nairn extraction.** Inspect the retained input,
  both rejected responses and the existing correction contract. Verify the
  supported endpoint/span or omission through the existing grounding owner and
  complete response validation. Change the shared prompt/validation owner only
  if a reproducible product defect is found. A valid refusal remains a content
  hold, not a reason to publish an unsupported claim.
- [x] **T4 — Qualify and repeat the bounded smoke.** Run affected regressions and
  the required local suite after product changes. On a disposable corpus copy,
  use normal configuration and ordinary selected-item replay; verify terminal
  outcome, lineage, receipts, source preservation, independent search coverage
  and normal retry. Record a fresh explicit scope before any hosted calls;
  historical five-minute windows have ended. Fiers is a preservation control,
  not unfinished work to redo. Do not claim Test 4 complete until the selected
  Nairn hold resolves and the configuration workaround is unnecessary.

## Starting evidence and ownership

- [Latest repeated smoke](smoke-repeat-20260914.md#test-4-content-and-replay-findings).
- [Replay defect and original workaround](test4-operational-redo-20260913.md#rag-smk-010--unrelated-source-addition-blocks-old-item-replay-open).
- At the starting baseline, `ragwork.replaydeadletters` compared whole configuration semantic
  hashes before selecting eligible items. Configuration compatibility belongs to
  `ragconfiguration`; prompt contracts and grounding belong to their existing domain
  owners in the [architecture map](architecture.md).
- Existing replay coverage was in `provider_durability_scenario.crexx` and
  `lifecycle_regression.crexx`; the additive-source case was absent.

## Implementation record

Started from clean `300c2ac` on 14 September. The user explicitly requested
implementation of T2–T4. Work remains on disposable copies, with no installation,
push or authoritative-library change.

T2 baseline: `configuration_contract` passes; the new `provider_durability`
acceptance fails exactly four positive cases: added source, unrelated source
edit, unused provider change and operational settings. Its eight meaningful
negative controls and the existing replay lifecycle checks pass. The first
fixture attempt omitted required priority; that setup was corrected before
recording the four actual defect failures in `baseline-replay.log`.

The first local panel passed 3/3. The real retained item then exposed a fixture
gap: work input stores `folder:SOURCE_ID`, not the bare source ID. The fixture
was corrected and reproduced four failures again before adding qualified-scope
matching in the shared projection. `baseline-qualified-scope.log` retains that
failure. This is why the real selected-item acceptance is required before T2
is ticked.

`ragcanonical` now supplies one semantic-field projection for typed current
configuration and frozen JSON, with optional source/role selection. The full
projection retains the existing semantic identity bytes, covered by the captured
baseline hash. `ragconfiguration` compares the selected distinct source/role
bindings and profile; `ragwork` composes that decision after checking selection
size. Snapshot reads use their primary keys; selected input reads use job/item
indexes. No full-corpus scan or per-item snapshot reread is introduced.

T3 inspection confirms the existing correction feedback already identifies
`relationships[4]`, both required endpoints and the rejected quotation. No prompt
or grounding-rule defect has been demonstrated. The corrected response draft
omits that single unsupported relationship and preserves the other 15 mentions,
eight relationships and two notes. The real native provider path, with only the
App Server response replaced by a local captured fixture, rejects the original
response on both attempts and accepts the corrected response in one attempt.
That first mechanical correction publishes 15 mentions and seven distinct new claims, resolves the selected
root through normal replay lineage, and retains all sources and chunks. This
is complete deterministic validation, not a hosted-model result. The corrected
job takes 4.032 seconds; the preceding failed job still activates available
vectors independently. See `captured-validation.json`.

A subsequent source-meaning review also removes weak membership/support and
mere adjacency inferences from that mechanical draft. The final
`nairn-reviewed.json` retains three explicitly named participants in the retreat
and the explicitly stated advanced-guard membership of the Argyleshire men.
On a separate fresh copy the final executable accepts all four relationships,
15 mentions and two notes, publishing four distinct claims in 36.136 seconds
(including the initial schema-19 index build). See `reviewed-validation.json`.
The earlier mechanical draft is retained only to isolate the reported endpoint
failure; the four-relationship draft is the reviewed content correction.

All six focused tests now have passing results: the initial panel passed five
and exposed a missing `ragconfiguration` dependency in the standalone lifecycle
VM harness; adding that dependency makes its unchanged acceptance pass on both
VMs (3.60 seconds). The current configuration JSON is reused when deriving its
semantic projection, avoiding duplicate serialization introduced by the refactor.
Final-artifact qualification and the live T4 result are recorded below.

## Fresh bounded live scope

The user's instruction to implement this checklist authorizes its selected
Test 4 repeat within the previously approved limits. Use only the original
public-history Nairn item and its existing public source/catalogue context on
the independent `live-library` copy recorded in `start.json`. Use the configured
managed Codex `gpt-5.6-luna` route, at most **two turns** (initial plus the normal
single correction), **five minutes** from launch, **zero Gemini calls** and
**zero monetary API cost**. The normal configuration retains two workers and
the unrelated Bannockburn source. Fiers is already complete and receives no
new call. No wider ingestion, other hold replay or installation is included.
Retain start/end times and actual terminal receipts; the live copy starts from
the original repeat state, with no synthetic fixture receipts or claims.

## Final result

| Check | Result |
| --- | --- |
| Final artifact | Native SHA-256 `88d257815970bbd786744c4c1ec48114060eb1ddf02356b4554ac2e74f8b9acd`; installed CREXX package unchanged. |
| Full local suite | **71/71, 612.88 seconds**. Two CTest slots with the prior temporary fixture/work-directory locks; generated test file restored exactly. Artifact unchanged throughout. |
| Normal replay | **0.355 seconds**, one Nairn item queued under `config-a211c0fede4237fe77bbc797`, including the unrelated Bannockburn source. No configuration workaround. |
| Live execution | **77.350 seconds**, one successful managed Codex turn; 19,464 input / 1,461 output tokens, 30.035 seconds recorded provider time, zero Gemini calls and zero monetary API cost. The launch/status interval is **10:18:16–10:19:34 UTC**, about 78 seconds. |
| Accepted content | Twelve grounded mentions and one supported claim: the duke's advanced guard **had-member** the Argyleshire men. |
| Normal review | The model also proposed membership of Clanranald and Appin. Both were automatically held as reviews, not published. Reading the complete sentence establishes that those regiments were ordered back to support the French. Both proposals were rejected with `review decide --decision reject --apply`; no new provider call or claim retraction was needed. |
| Retry and preservation | Normal completed-job retry **5.655 seconds**, zero new calls or attempts. A further replay of the completed root is rejected. Original Fiers/Nairn item and attempt histories are byte-identical; both roots are resolved. |
| Source/search coverage | All nine source records, nine revisions and 34,907 chunks are byte-identical. All 34,907 vectors remain available; the stored index marker is clean. Focused lexical retrieval finds the Nairn passage in **1.042 seconds**; citation resolution **0.457 seconds**, independently matched to original UTF-8 bytes **842394–843715**. |
| Final verification | Schema 19, generation 24,929, aligned manifest, zero storage/repository issues. Full verification took **66.539 seconds**; this is an integrity scan, not a routine QA setup operation. |
| Final accounting | Exactly **82,562 → 82,563 provider runs** and **116,206 → 116,207 attempts**. No unsettled budget reservations or surviving selected controllers/workers. Original extraction roots: **543 actionable / two resolved**. |

This is a completed operational smoke with normal review handling, not a claim
that every model proposal was correct automatically. The two incorrect proposals
never entered the accepted graph. The earlier manual four-relationship draft
was validated only in its independent local fixture copy; it was not substituted
for the live response or copied into the live ledger.

The live copy is `/private/tmp/crexxrag-test4-repair-kw6or_2x/live-library`.
The normal installation, master corpus and permanent ScottishHistory query copy
are unchanged. No push occurred. The bounded live window has ended; do not
restart other historical work from these saved plans.

Evidence is retained in [the evidence directory](qa/test4-repair-20260914/README.md).
