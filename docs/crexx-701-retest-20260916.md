# CREXX #701 — installed runtime retest, 16 September 2026

Adrian requested focused downstream verification of the updated CREXX package
already installed in `/Users/adrian/.local`. No CREXX checkout changes, remote
publication, operator installation, corpus operation or hosted calls are part of
this request. The existing dirty maintenance/escalation and QA-redesign batch is
preserved at RAG base `5aad6706102c2ee35faf38020f8859cc4ef8afd7`.

## Candidate and scope

Installed `BUILDINFO` identifies clean CREXX
`17e844441ed87e1f6e0d5f1f0d3bb4bee8db6187`, display
`crexx-1.0.0-beta.3+local.g17e844441ed8`, timestamp `20260916T133711Z`.
The local CREXX repository's `origin/develop` tracking ref also points to that
revision; this is a local observation, not a fresh remote fetch. Its checked-in
qualification record identifies `135b9254f` as the final runtime/test code and
subsequent revisions as documentation follow-ups.

The RAG native executable statically includes CREXX runtime code. Rebuilding it
against the installed package is therefore required; rerunning the old executable
would not verify this fix. Only `crexxrag_native` and `crexxrag_product` targets
are needed for this panel. No RAG product source or acceptance assertions change.

`worker_unexpected_exit` is re-enabled, with eight scheduling slots and its
original three fault cases:

1. `worker-kill`: kill a worker before provider submission; replace its slot
   while all seven peers remain alive and undrained, then complete the work.
2. `worker-unknown`: kill after submission; replace capacity while preserving
   the killed task's unknown outcome and preventing duplicate submission.
3. `worker-write-error`: fail persistence of the exit; report the failure and
   avoid pretending a replacement was safely recorded.

The assertions independently check persisted runtime rows, attempts, provider
intents/outcomes, replacement count, cumulative budgets, outstanding reservations
and SQLite integrity where applicable. `native_supervision_positive` is the
ordinary replacement control; `process_workers` checks both installed VMs and
eight-worker process coordination. All inputs and libraries are local scratch
fixtures. The full suite is not repeated for this focused verification.

## Results

**Pass.** `worker_unexpected_exit` executes all three unchanged fault cases and
passes in **6.87 seconds**. Each retained `observer.log` reports `exit=0`.
The original assertions establish prompt replacement without draining healthy
peers, exactly one submitted intent for the killed item, the uncertain-outcome
hold, unchanged budgets and the persistent exit-write error path.

The two focused controls also pass: `native_supervision_positive` and
`process_workers` (both installed VMs), **2/2 in 32.40 seconds**. Combined focused
test wall time is **39.27 seconds**, excluding rebuild and documentation checks.
Normal configure/build succeeds, including the native packager's library-init
and two-worker supervision smoke. The documentation contract and whitespace
check are the final small follow-ups. No acceptance assertion was weakened or
removed, and no RAG product source was edited for this verification.

Tested native SHA-256:
`8e969d67f883ef67ea00933e1013985df94069c487c1a3fb4e4c887bd36b9a4e`.
Linked bytecode SHA-256:
`3dc07d5fe67e6969a403e6635b20ab9085009591d95b916f031acba29caebe0e`.
Installed `BUILDINFO` SHA-256:
`50ec58e52d5b4e23238ce6c9c54a612028a7e3bebe199110a78a71c77426b940`.
The native build log records linkage to runtime libraries and providers under
`/Users/adrian/.local/bin`; the installed package identity is checked again at
closeout. The tested product has not been installed into the operator prefix.

The temporary `DISABLED TRUE` and `known-upstream` suppression are removed from
`worker_unexpected_exit`; its `crexx-701` label remains for focused selection.
It requests eight scheduling slots, matching the actual worker cohort.

Evidence is retained under [the retest evidence directory](qa-evidence/20260916-crexx-701/results.json):
configuration/build and CTest logs, native link log, installed BUILDINFO, current
result signatures and per-fault observer/controller/descriptor evidence. Tests
use fresh private scratch directories and no hosted provider calls.

Earlier full-suite results and the previous #701 exclusion remain historical
evidence for their original runtime/artifacts. This focused verification does
not claim a fresh full-suite, endurance or cross-platform pass on the updated
runtime. The known downstream #701 regression now passes and stays enabled.
