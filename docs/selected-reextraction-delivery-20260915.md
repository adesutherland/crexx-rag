# FR-001 — bounded selected fresh extraction

## Vision and accepted authority

Adrian's retry-first escalation workflow needs selected old failed cases
evaluated under current prompts before their content failures can be called
current defects. The coordinator authorized implementation of the narrow
selected-work extension on 15 September. Keep it separate from ISSUE-01 worker
replenishment and ESC-001 terminal no-change decisions.

Create fresh work from exact immutable selected chunks and current configuration,
prompt and profile. Freeze explicit item/call/time limits in a reviewable plan;
apply only its exact digest with recorded manager-authorized reopening. Preserve
old failed items, referrals, accepted decisions, evidence and unrelated holds.
Cancellation, pending review, active ownership and uncertain provider outcomes
must not be silently bypassed. Use existing maintenance plan/apply and worker
owners rather than a new orchestration framework. Normal same-interpretation
replay retains its guard.

Core-only implementation, isolated fixtures and QA are authorized. No master
mutation, restart or installation. No historical model failure is considered
reproduced until this new route actually evaluates it with current prompts.

## Verified gap

The coordinator's fresh `job replay --item` returned exit 6, `replay target
changes the source, provider or interpretation of selected work`. Scoped retry
reset succeeded (one job, 28001 items, no calls), and replay still refused:
attempt eligibility and interpretation compatibility are separate controls.
Receipts are under
`/Users/adrian/Documents/ScottishHistory/reports/escalation-review-20260915/evidence/`:
`old-case-replay-1.json`, `old-job-reset-retries.json`,
`old-case-replay-after-reset.json`.

Installed public alternatives do not meet the requested scope: source-scoped
maintenance is broader and retains referral holds; task retry retains those
holds; `maintain plan --input` currently requires provenance enrichment; external
task resolution produces lifecycle decisions, not fresh extraction work.
That code is unchanged by the current worker repair. The gap and minimal plan
extension were reported to the coordinator before implementation authority.

## Acceptance checklist

- [x] Confirm the public capability gap and preserve old-content uncertainty.
- [ ] Inspect plan/backlog/work/receipt owners and baseline public journeys;
  identify shared input construction and lineage/closure behavior to reuse.
- [ ] Add regression-first changed-prompt selected retry and successful controls;
  assert exact selected chunks/current interpretation, digest and bounded calls.
- [ ] Add rejection/hold cases for unselected work, cancellation, pending review,
  active ownership and uncertain outcomes; ordinary replay still rejects changes.
- [ ] Implement the narrow worklist plan/apply path and explicit manager
  reopening with shared validation and immutable original records.
- [ ] Prove fresh success closes/supersedes the original referral with provenance;
  failed fresh work remains actionable and never falsely resolves its old case.
- [ ] Prove routine census/retry cannot dispatch escalations automatically, and
  retries stay within the approved fresh-work limits.
- [ ] Update CLI/MCP catalogue, agent/user/architecture guidance and coverage;
  run targeted/full QA and return the exact tested command workflow.

Status: implementation authority withdrawn by Adrian's subsequent correction.
Stop this extension and investigate existing-capability workarounds first.
Only this requirement record was created; no FR-001 product/test edits, install,
publication or master action occurred. Preserve the proposal for reference.
Subsequent bounded worker-replacement and task-reset repairs have their own
completed delivery records; this broader extension remains withdrawn.
