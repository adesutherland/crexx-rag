Discover zero or more concepts, directional typed relationships, and important grounded analysis notes in the supplied evidence. Return every directly supported relationship up to the stated bounds, not just one. Provide evidence_quote copied from the chunk for each mention, relationship and note; the application computes UTF-8 byte offsets. Mention labels must occur in their quotation; relationship quotations must contain both endpoint labels. Use only allowed concept and relationship types. Do not invent evidence. During maintenance, use the durable query-gap and prior-note context to investigate omissions and important follow-up work, but ground every returned note in a quotation from the supplied text. For synonym, split, merge, type-correction, retire or restore notes, next_action must be canonical compact JSON with schema crexx-rag.lifecycle-action/1 and exactly operation, concept_id, target_concept_id, alias, concept_type, successors, dispositions and reason. Use exact catalogue concept ids from the durable input. Initial split/merge suggestions use an empty dispositions array; maintenance later enumerates every affected object before any connection can move.

Before returning, check the whole discovery batch against the supplied text, glossary, vocabulary and bounds:
1. Keep mentions, relationships and notes within their respective maximum counts. These are ceilings, not targets. Prioritise useful, directly supported content; preserve supported relationships within the bounds. Return empty arrays where nothing is supported, not as a substitute for examining the passage. A useful isolated domain concept may be retained without inventing a relationship. A page marker, running header, adapter token or locator is provenance metadata, not automatically a domain concept.
2. Copy each evidence_quote as one continuous span of the supplied chunk, preserving spelling, OCR, punctuation, spaces and line breaks. Do not join fragments, repair line-end hyphenation, paraphrase or copy from a different source. JSON-escape once. Each mention label must occur in its own quotation; prefer the actual source surface over an expanded title absent from the text.
3. Select the quotation before the relationship. Both final endpoint labels must occur in that one span, and the passage must support the allowed relation and its direction. Co-occurrence, a document title, or a pronoun standing for an absent endpoint is insufficient. Read negation, contrasts such as "rather than", conditions, recommendations, attribution, dates and locality: do not turn them into an unconditional or reversed claim. Do not force a domain-specific claim into an unrelated allowed relationship type. A qualified, grounded note may preserve useful meaning that the relationship vocabulary cannot express.
4. Aliases must name the same referent. An organisation's abbreviation is not an alias for its publication, service or title merely because it appears there. Respect reviewed glossary identities when the referent actually matches; distinguish an entity from a document it produced, a person from a place, and an activity from its locality or responsible actor where the permitted types make those distinctions. Do not override a reviewed identity to make a proposed alias fit.
5. Check that each relationship's zero-based indexes select the intended final mentions. If a mention is removed, remove unsupported dependent relationships and update remaining indexes. Use only the supplied types and exact catalogue IDs where required.
6. Keep descriptive annotations separate from actionable questions. Use an actionable note only when a specific, evidence-seeking next step is warranted; an observation need not create a new maintenance lead.
On correction, reconsider the meaning and identity of the entire result, not only the reported field. Repair a quote or ID only when the underlying claim remains supported. Remove unsupported components while retaining useful supported content; do not enlarge or shorten quotations to conceal missing justification.
