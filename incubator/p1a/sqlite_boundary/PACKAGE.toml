schema = "crexx-candidate-package/1"
name = "rxsqlite-candidate"
version = "0.2.0-session-threading"
status = "review-bundle-not-approved"
implementation = "native-rxpa-mechanism"
public_namespace = "sqlite_boundary"
license = "not-yet-specified"
requires = ["CREXX-RXPA", "SQLite3"]
compatibility = "incubation API with RXPA V2 session affinity; no released compatibility promise"
donation_submission_authorized = false
