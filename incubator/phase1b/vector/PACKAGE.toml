schema = "crexx-candidate-package/1"
name = "rxvector-portable-candidate"
version = "0.1.0-phase2"
status = "review-bundle-not-approved"
implementation = "crexx-level-g-library"
public_namespaces = ["vector_codec", "vector_search"]
license = "not-yet-specified"
requires = ["rxfnsb"]
compatibility = "f32le-v1 is retained; module API remains incubation-only"
donation_submission_authorized = false
